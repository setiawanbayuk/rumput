<button class="btn btn-secondary btn-sm" data-id="{{ $id }}" data-bs-toggle="modal" data-bs-target="#registerModal"
    @if ($status != '4' && $role == '6') disabled @endif>
    <i class="ri-arrow-up-double-fill" data-bs-toggle="tooltip" data-bs-title="Esign" title="Esign"></i>
</button>
